[project]
name = "a2a-barebone"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "a2a-sdk>=1.0.3",
    "sse-starlette>=3.4.4",
    "starlette>=1.0.0",
    "uvicorn>=0.47.0",
]
